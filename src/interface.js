// The goal of this document is to act as a bridge between backend model and front end html layer. Using jQuery to target html elements and populate them using methods created in business logic files

$( document ).ready(function() {
  var countMeUp = new CountMeUp();
  var user = new User();
  var candidate = new Candidate();

  

})
